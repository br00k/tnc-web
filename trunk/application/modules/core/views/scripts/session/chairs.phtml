<?= $this->render('session/subnav.phtml') ?>
<? #if ($this->reviewers): ?>
<table class="reviewers">

<tr><td>
<?= $this->sessionUserForm; ?>
</td><td>&nbsp;</td>
</tr>
</table>
<? #endif; ?>
<? if ($this->authInfo()->isAdmin() ): ?>
<p>This list only shows session chairs. To add a user to this list, add the role 'chair' to an existing user or <a href="/core/user/new/role_id/6">send out invite</a></p>
<? else: ?>
<p>Can't find a session chair in the list? Send an email to <a href="mailto:<?= $this->conferenceInfo('email') ?>">the administrator</a></p>
<? endif; ?>